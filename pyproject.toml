[project]
name = "serif-takehome"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "json-stream>=2.4.1",
    "pandas>=3.0.0",
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "pandas>=3.0.0",
]
