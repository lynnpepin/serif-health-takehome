[project]
name = "serif-takehome"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.0",
]

[dependency-groups]
dev = [
    "pandas>=3.0.0",
]
